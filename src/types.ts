/**
 * This file defines the types that will be available in the public API of {{npm-package-name}}.
 *
 * E.g. `import { NpmPackageNameOptions } from 'npm-package-name`
 */

export type { NpmPackageName, NpmPackageNameOptions } from './npm-package-name/types'
